# Risk Test: F14 — Long / complex input (reduced from full brief to control output size)
# Expectation: parse_error false; full schema. IDP + many integrations in fewer words.

Build an internal developer platform (IDP) for a 200-person engineering org (12 product + 3 platform squads, ~140 microservices / ~340 GitHub repos, no unified DX today).

**Must deliver:** Service catalogue (auto-discover repos; owner, SLOs, deps, deploys, runbooks; search). Self-serve provisioning (Postgres, Redis, S3, SQS, ECS) via Terraform + approval + cost estimate. Onboarding: squad checklist, auto-provision GitHub/IAM/Slack/Jira. Incident view: PagerDuty → service page (owner, deploys, deps, runbook, history). Plugins via REST + webhooks (security scans, DORA, etc.).

**Integrations (must support):** GitHub, AWS (IAM, ECS, RDS, S3, SQS, CloudWatch), Terraform Cloud, PagerDuty, Jira, Confluence, Slack, Okta SSO, Datadog.

**NFR:** Catalogue search p95 < 1.5s; 99.9% uptime; squad-level RBAC for cost/provisioning; 2y audit log for provision actions.

**Constraints:** 9 months to first internal full launch; $850k budget (incl. 12mo infra). Team: 4 BE, 2 FE, 1 DevOps/platform, 1 security, 1 UX, 1 PM, 0.5 data engineer (shared). Do not replace PagerDuty or Jira — integrate only.
