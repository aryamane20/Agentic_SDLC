# Project 2 API — stack per docs/ARCHITECTURE.md §6
# Install from repo root: pip install -r requirements.txt -r backend/requirements.txt
# Run API: uvicorn backend.api.main:app --reload --port 8000
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
