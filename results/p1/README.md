# P1 results

| Path | Contents |
|------|----------|
| **`eval/`** | Scorecard JSON from **`python scripts/run_eval.py`** (dimensions, pass/fail, cost). New runs write here — **not** `eval/results/`. |

Agent-cached report JSON per test case still lives under **`outputs/{prompt_version}/`** (gitignored, regenerate with `--generate`).

Optional: copy snapshots of `outputs/` here for milestones (`snapshots/`).
